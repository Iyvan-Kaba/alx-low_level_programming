#include "main.h"
/**
 * _strpbrk -> a function that searches a string
 * @s: a string
 * @accept: a character
 * Return: a string
 */
char *_strpbrk(char *s, char *accept)
{
	return (_strpbrk(s, accept));
}
