#include "main.h"
/**
 *add - adds two integers and returns the result.
 *@int: is a parameter for the sum value of integers.
 *Return - always returns value of 'add' x
 */
int add(int, int)
{	
	int a;
	int b;
	int x;

	x = a + b;
	return (x);
}
