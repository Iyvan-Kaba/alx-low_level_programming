#include "main.h"
/**
 * _isupper -> this is a function to check upper case alpahbets
 * @c: integer parameter
 * Return: always 0 on success
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);

return (0);
}
