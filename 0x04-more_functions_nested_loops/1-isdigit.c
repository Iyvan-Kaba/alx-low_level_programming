#include "main.h"
/**
 *_isdigit -> checks for a digit (0 through 9).
 *@c: is a parameter
 * Return: returns 1 if c is a digit and 0 otherwies
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
